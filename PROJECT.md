# 🎵 Сайт рецензирования музыки - Полная документация проекта

> **Единый файл с актуальной информацией о проекте: ТЗ, статус, API, установка**

---

## 📋 Содержание

1. [Техническое задание](#техническое-задание)
2. [Статус проекта](#статус-проекта)
3. [API документация](#api-документация)
4. [Установка и запуск](#установка-и-запуск)
5. [Структура проекта](#структура-проекта)

---

## 🎯 Техническое задание

### Цель проекта
Создать учебный веб-проект (уровень **4 курса**) для **рецензирования музыкальных альбомов и треков**. Пользователь может просматривать альбомы и отдельные треки, оставлять отзывы и оценки, а также взаимодействовать с рецензиями других пользователей.

### Технологический стек

**Frontend:**
- React (JavaScript)
- Самописные CSS стили с палитрой Solarized (темная/светлая тема)
- Адаптивная верстка
- Компонентный подход

**Backend:**
- Go (Golang) + Gin framework
- GORM для работы с БД
- PostgreSQL
- Header-based аутентификация (X-User-ID)

### Система оценки

Рецензии оцениваются по специальной системе с 4 базовыми критериями (1-10 баллов) и множителем "Атмосфера/Вайб" (1.0000-1.6072).

#### Оценочные параметры (базовые критерии):

1. **Рифмы / Образы (1–10):**
   Оценка за текст, учитывающая жанровые особенности. Для легкой фоновой поп-музыки или электронной музыки допускаются тривиальные конструкции для максимального балла. Для текстоцентричных жанров требуется сложная рифмовка и глубокий смысл.

2. **Структура / Ритмика (1–10):**
   Включает оценку ритмической составляющей (стихотворный ритм — мелодичность, драматургия частей, контрасты) и гармонию структуры (целостность всех частей трека, концепция альбома и расположение песен).

3. **Реализация стиля (1–10):**
   Оценивает работу исполнителя (качество вокала, речитатив, умение работать с мелодией) и работу звукорежиссера/саунд-продюсера (качество сведения, звучание инструментала).

4. **Индивидуальность / Харизма (1–10):**
   Оценивает уникальность тембра голоса, стиль исполнения, узнаваемость, а также способность артиста передать и погрузить слушателя в эмоции ("верю — не верю" песне).

5. **Атмосфера / Вайб (Множитель) (1-10):**
   Диапазон: от ×1.0000 до ×1.6072 (с точностью до 4 знаков после запятой).
   Субъективная оценка, показывающая, насколько автор сумел передать атмосферу и палитру эмоций композиции.

#### Формула подсчёта итогового балла:
```
(Рифмы/Образы + Структура/Ритмика + Реализация стиля + Индивидуальность/Харизма) × 1.4 × Атмосфера/Вайб
```

**Пример:** Если пользователь выставил 10 баллов во всех базовых категориях и вайб = 1.6072:
```
(10+10+10+10) × 1.4 × 1.6072 = 40 × 1.4 × 1.6072 = 89.99
```

### Основной функционал

1. **Главная страница** - список альбомов (карточки с обложкой, жанром и рейтингом), блоки популярных треков и рецензий
2. **Страница альбома** - отображение обложки, описания, списка треков и рецензий
3. **Страница трека** - детальная информация о треке, рецензии, ссылка на альбом
4. **Регистрация и авторизация** - создание пользователя, вход/выход
5. **Профиль пользователя** - просмотр собственных рецензий, редактирование и удаление
6. **Фильтры и сортировка** - по жанру, рейтингу, названию
7. **Панель администратора** - модерация рецензий, управление контентом
8. **Система лайков** - лайки для альбомов, треков и рецензий
9. **Темная/светлая тема** - переключатель темы с сохранением в localStorage
10. **Поиск** - поиск с автодополнением по названию/исполнителю

---

## 📊 Статус проекта

### ✅ Что готово

**Backend (Go + Gin + GORM):**
- ✅ Полная архитектура с разделением на слои
- ✅ Все модели данных (User, Album, Track, Review, Genre, Likes)
- ✅ Автоматическое создание БД и миграции
- ✅ Seed данные (тестовые пользователи и альбомы)
- ✅ Аутентификация (регистрация, вход, header-based auth)
- ✅ Полный REST API (CRUD для всех сущностей)
- ✅ Валидация входных данных
- ✅ Централизованная обработка ошибок
- ✅ Middleware (авторизация, проверка прав администратора)
- ✅ CORS настроен
- ✅ Автоматический расчет рейтингов
- ✅ Система лайков (альбомы, треки, рецензии) с тестовыми данными (5-20 лайков на треки от разных пользователей)
- ✅ Поиск с автодополнением
- ✅ Рецензии на альбомы и треки
- ✅ Модерация рецензий
- ✅ Полная загрузка данных пользователя в GetUserReviews (Preload User, Album, Track, Likes)

**Frontend (React):**
- ✅ Компонентная архитектура
- ✅ React Router настроен
- ✅ Аутентификация (AuthContext, формы входа/регистрации)
- ✅ API сервисы (axios с interceptors)
- ✅ Все основные страницы (главная, альбом, трек, профиль, админ-панель)
- ✅ Все компоненты (Header, AlbumCard, TrackCard, ReviewCard, ReviewCardSmall, LikeButton, ReviewForm, Filters)
- ✅ Самописные CSS стили с палитрой Solarized
- ✅ Темная/светлая тема с переключателем
- ✅ Адаптивный дизайн
- ✅ Система оценки с автоматическим расчетом
- ✅ Система лайков (UI + функционал)
- ✅ Поиск с автодополнением в Header
- ✅ Блоки на главной странице (популярные треки, последние релизы, популярные рецензии 3x2)
- ✅ Информация об альбоме/треке в рецензиях профиля с кликабельными ссылками
- ✅ Редактирование рецензий без текста (кнопки всегда доступны если canEdit)

**База данных:**
- ✅ Все таблицы спроектированы
- ✅ Автоматические миграции через GORM
- ✅ Все foreign keys настроены
- ✅ Уникальные индексы для лайков

### ⚠️ Что не готово / Требует доработки

- ⚠️ **JWT токены** - используется header-based auth (X-User-ID), можно улучшить до JWT
- ⚠️ **Загрузка файлов** - нет функционала загрузки обложек (только отображение существующих)
- ⚠️ **Комментарии** - система комментариев под рецензиями не реализована
- ⚠️ **Тестирование** - нет unit-тестов и интеграционных тестов
- ⚠️ **Swagger/OpenAPI** - нет автоматической генерации документации
- ⚠️ **Комментарии в коде** - минимальные, можно добавить больше

### 🎯 Готовность к демонстрации

**Оценка: ~99%**

**Что работает:**
- ✅ Регистрация и вход
- ✅ Просмотр альбомов с фильтрацией
- ✅ Создание рецензий с системой оценки
- ✅ Модерация рецензий (для админа)
- ✅ Профиль пользователя
- ✅ Адаптивный дизайн
- ✅ Темная/светлая тема
- ✅ Система лайков
- ✅ Поиск с автодополнением

**Что нужно проверить перед демо:**
- ⚠️ Добавить несколько тестовых рецензий
- ⚠️ Проверить все сценарии использования
- ⚠️ Убедиться что нет критичных багов
- ⚠️ Подготовить демо-сценарий

---

## 🎯 API документация

### База URL
Все API endpoints доступны по адресу: `http://localhost:8080/api`

### Авторизация
Для доступа к защищенным эндпоинтам необходимо передавать заголовок `X-User-ID` с ID пользователя.

### Аутентификация
- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получение информации о текущем пользователе

### Альбомы
- `GET /api/albums` - Получение списка альбомов (с фильтрацией, сортировкой и пагинацией)
- `GET /api/albums/:id` - Получение детальной информации об альбоме
- `POST /api/albums` - Создание нового альбома (авторизованный пользователь)
- `PUT /api/albums/:id` - Обновление альбома (только админ)
- `DELETE /api/albums/:id` - Удаление альбома (только админ)
- `POST /api/albums/:id/like` - Лайк альбома
- `DELETE /api/albums/:id/like` - Убрать лайк с альбома

### Рецензии
Рецензии могут быть созданы как для альбомов, так и для треков. При создании необходимо указать либо `album_id`, либо `track_id` (но не оба одновременно).

- `GET /api/reviews` - Получение списка рецензий (с фильтрацией по `album_id`, `track_id`, `user_id`, `status`)
- `GET /api/reviews/popular` - Получение популярных рецензий за последние 24 часа (только с альбомами, сортировка по лайкам)
- `GET /api/reviews/:id` - Получение детальной информации о рецензии
- `POST /api/reviews` - Создание новой рецензии
- `PUT /api/reviews/:id` - Обновление рецензии (автор или админ)
- `DELETE /api/reviews/:id` - Удаление рецензии (автор или админ)
- `POST /api/reviews/:id/like` - Лайк рецензии
- `DELETE /api/reviews/:id/like` - Убрать лайк с рецензии
- `POST /api/reviews/:id/approve` - Одобрение рецензии (только админ)
- `POST /api/reviews/:id/reject` - Отклонение рецензии (только админ)

### Треки
Треки могут иметь несколько жанров (many-to-many связь).

- `GET /api/tracks/popular` - Получение популярных треков за последние 24 часа
- `GET /api/tracks/:id` - Получение трека по ID
- `GET /api/albums/:id/tracks` - Получение треков альбома
- `POST /api/tracks` - Создание трека (только админ)
- `PUT /api/tracks/:id` - Обновление трека (только админ)
- `DELETE /api/tracks/:id` - Удаление трека (только админ)
- `POST /api/tracks/:id/like` - Лайк трека
- `DELETE /api/tracks/:id/like` - Убрать лайк с трека

### Жанры
- `GET /api/genres` - Получение списка жанров
- `GET /api/genres/:id` - Получение жанра по ID
- `POST /api/genres` - Создание жанра (только админ)
- `PUT /api/genres/:id` - Обновление жанра (только админ)
- `DELETE /api/genres/:id` - Удаление жанра (только админ)

### Поиск
- `GET /api/search?q=query` - Поиск альбомов и треков с автодополнением (возвращает до 5 альбомов и 5 треков)

### Пользователи
- `GET /api/users/:id` - Получение пользователя по ID
- `GET /api/users/:id/reviews` - Получение рецензий пользователя
- `PUT /api/users/:id` - Обновление профиля пользователя
- `DELETE /api/users/:id` - Удаление пользователя

### Работа с изображениями

Обложки альбомов и треков хранятся в поле `cover_image_path`.

**Формат пути:**
- Путь к изображению указывается относительно корня проекта (например, `/preview/1.jpg`)
- Изображения должны находиться в папке `frontend/public/`
- URL для доступа: `http://localhost:3000/preview/1.jpg`

**Обработка ошибок:**
- Если изображение не найдено, отображается placeholder (🎵)
- Все компоненты используют утилиту `getImageUrl()` для формирования правильных URL

---

## 🚀 Установка и запуск

### Требования
- **Go 1.21+** - [Скачать](https://golang.org/dl/)
- **Node.js 16+** - [Скачать](https://nodejs.org/)
- **PostgreSQL 12+** - [Скачать](https://www.postgresql.org/download/)

### Быстрая установка

#### 1. Подготовка базы данных
Убедитесь, что PostgreSQL запущен. База данных создается автоматически при первом запуске backend.

#### 2. Настройка Backend

```bash
cd backend
go mod tidy
```

Создайте файл `.env` в папке `backend/`:
```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=music_review_db
DB_SSLMODE=disable
PORT=8080
GIN_MODE=debug
```

Запустите сервер:
```bash
go run main.go
```

Backend будет доступен на `http://localhost:8080`

#### 3. Настройка Frontend

Откройте **новый терминал**:

```bash
cd frontend
npm install
```

Создайте файл `.env` в папке `frontend/`:
```env
REACT_APP_API_URL=http://localhost:8080/api
DANGEROUSLY_DISABLE_HOST_CHECK=true
```

Запустите приложение:
```bash
npm start
```

Frontend будет доступен на `http://localhost:3000`

### Тестовые пользователи

После первого запуска автоматически создаются:

- **Администратор:**
  - Email: `admin@example.com`
  - Пароль: `admin123`

- **Тестовый пользователь:**
  - Email: `test@example.com`
  - Пароль: `test123`

### Проверка работы

1. Откройте `http://localhost:3000` в браузере
2. Войдите с тестовыми данными
3. Проверьте главную страницу с альбомами
4. Откройте альбом и создайте рецензию
5. Войдите как администратор и проверьте модерацию

### Возможные проблемы

**Backend не запускается:**
- Проверьте, что PostgreSQL запущен
- Проверьте правильность данных в `.env` (особенно `DB_PASSWORD`)
- Убедитесь, что порт 8080 свободен

**Frontend не подключается к Backend:**
- Проверьте, что Backend запущен на порту 8080
- Проверьте переменную `REACT_APP_API_URL` в `.env` frontend
- Убедитесь, что оба сервера работают одновременно

---

## 📁 Структура проекта

```
music-review-site/
├── backend/                 # Backend приложение (Go)
│   ├── main.go             # Точка входа
│   ├── routes/             # Маршруты API
│   ├── controllers/        # Контроллеры
│   ├── models/             # Модели данных
│   ├── middleware/         # Middleware
│   ├── database/           # Настройка БД и миграции
│   └── utils/              # Утилиты
├── frontend/               # Frontend приложение (React)
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── pages/          # Страницы
│   │   ├── services/       # API сервисы
│   │   ├── context/        # React Context
│   │   └── utils/          # Утилиты
│   └── public/             # Публичные файлы (обложки)
├── README.md               # Главный файл проекта
├── PROJECT.md              # Полная документация (этот файл)
└── Tasks.md                # Задачи от пользователя
```

---

**Последнее обновление:** Все основные функции реализованы, проект готов к демонстрации (~99%). 

**Последние улучшения:**
- Добавлены лайки на треки (5-20 лайков от всех тестовых пользователей, распределение по разным артистам)
- Исправлен баг "неизвестный пользователь" в профиле (добавлен Preload("User") в GetUserReviews)
- Добавлена возможность редактировать рецензии без текста (кнопки показываются всегда если canEdit)
- Добавлена информация об альбоме/треке в рецензиях профиля с кликабельными ссылками
- Улучшена загрузка данных в GetUserReviews (Preload User, Album, Track, Likes для полной информации)
- Исправлены цвета на страницах альбомов и треков (палитра Solarized), исправлено отображение рецензий на главной странице
