# 🚀 Инструкция по запуску проекта

## Требования

- **Go 1.21+** - [Скачать](https://golang.org/dl/)
- **Node.js 16+** - [Скачать](https://nodejs.org/)
- **PostgreSQL 12+** - [Скачать](https://www.postgresql.org/download/)

## Быстрый старт

### 1. Подготовка базы данных

Убедитесь, что PostgreSQL запущен и доступен.

### 2. Настройка Backend

**Важно:** Все команды для backend нужно выполнять из папки `backend/`

```bash
cd backend
```

Создайте файл `.env` в папке `backend/`:

```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=music_review_db
DB_SSLMODE=disable

PORT=8080
GIN_MODE=debug
```

**Важно:** Измените `DB_PASSWORD` на ваш пароль от PostgreSQL.

Установите зависимости и запустите (убедитесь, что вы в папке `backend/`):

```bash
# Проверьте, что вы в правильной папке (должен быть файл go.mod)
go mod tidy
go run main.go
```

**Примечание:** Если видите ошибку "go.mod file not found", значит вы не в папке `backend/`. Выполните `cd backend` перед командами.

Backend будет доступен на `http://localhost:8080`

**Примечание:** База данных создается автоматически при первом запуске. Тестовые данные (пользователи и альбомы) также создаются автоматически.

### 3. Настройка Frontend

Откройте новый терминал:

```bash
cd frontend
```

Создайте файл `.env` в папке `frontend/`:

```env
REACT_APP_API_URL=http://localhost:8080/api
```

Установите зависимости и запустите:

```bash
npm install
npm start
```

Frontend будет доступен на `http://localhost:3000`

## Тестовые пользователи

После первого запуска автоматически создаются:

- **Администратор:**
  - Email: `admin@example.com`
  - Пароль: `admin123`

- **Тестовый пользователь:**
  - Email: `test@example.com`
  - Пароль: `test123`

## Проверка работы

1. Откройте `http://localhost:3000`
2. Войдите с тестовыми данными
3. Проверьте главную страницу с альбомами
4. Откройте альбом и создайте рецензию
5. Войдите как администратор и проверьте модерацию

## Возможные проблемы

### Backend не запускается

- Проверьте, что PostgreSQL запущен
- Проверьте правильность данных в `.env`
- Убедитесь, что порт 8080 свободен

### Frontend не подключается к Backend

- Проверьте, что Backend запущен на порту 8080
- Проверьте переменную `REACT_APP_API_URL` в `.env`
- Проверьте CORS настройки в `backend/main.go`

### База данных не создается

- Убедитесь, что у пользователя PostgreSQL есть права на создание БД
- Проверьте логи в консоли Backend

