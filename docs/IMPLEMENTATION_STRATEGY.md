# 🎯 Стратегия реализации новых функций

## 📋 Обзор задач

1. ✅ Актуализировать PROJECT.md (выполнено)
2. ✅ Разработать стратегии и TODO (выполняется)
3. ⏳ Реализовать просмотр профилей других пользователей
4. ⏳ Реализовать карточку артиста
5. ⏳ Реализовать расширенный поиск
6. ⏳ Добавить тестовые данные
7. ⏳ Актуализировать PROJECT.md

---

## 🎯 Задача 3: Просмотр профилей других пользователей

### Backend изменения:
- ✅ API уже есть: `GET /api/users/:id` и `GET /api/users/:id/reviews`
- Никаких изменений не требуется

### Frontend изменения:

#### 3.1. Создать UserProfilePage.js
- **Файл:** `frontend/src/pages/UserProfilePage.js`
- **Функционал:**
  - Отображение профиля любого пользователя по ID
  - Список рецензий пользователя
  - Без возможности редактирования (только просмотр)
  - Кнопка "Назад" или хлебные крошки

#### 3.2. Обновить ReviewCard.js
- **Изменения:**
  - Сделать ник пользователя (`review.user?.username`) кликабельным
  - Сделать аватар кликабельным
  - При клике переходить на `/users/:id`

#### 3.3. Добавить роут в App.js
- **Роут:** `<Route path="/users/:id" element={<UserProfilePage />} />`

#### 3.4. Создать UserProfilePage.css
- Стили аналогичные ProfilePage.css, но без кнопки редактирования

---

## 🎯 Задача 4: Карточка артиста

### Backend изменения:

#### 4.1. Добавить endpoint для получения альбомов артиста
- **Файл:** `backend/controllers/album_controller.go`
- **Метод:** `GetAlbumsByArtist(c *gin.Context)`
- **Endpoint:** `GET /api/albums/artist/:artistName`
- **Функционал:**
  - Получение всех альбомов по имени артиста
  - Сортировка по дате выхода (если есть) или по created_at
  - Preload Genre и Likes

#### 4.2. Добавить роут
- **Файл:** `backend/routes/routes.go`
- **Роут:** `albums.GET("/artist/:name", albumController.GetAlbumsByArtist)`

### Frontend изменения:

#### 4.3. Создать ArtistPage.js
- **Файл:** `frontend/src/pages/ArtistPage.js`
- **Функционал:**
  - Отображение имени артиста
  - Список всех альбомов артиста (использовать AlbumCard)
  - Сортировка альбомов
  - Статистика (количество альбомов, средний рейтинг)

#### 4.4. Сделать имена артистов кликабельными
- **Файлы для обновления:**
  - `AlbumCard.js` - имя артиста в карточке
  - `AlbumDetailPage.js` - имя артиста на странице альбома
  - `TrackCard.js` - имя артиста в карточке трека
  - `ReviewCard.js` - имя артиста в рецензии (уже есть ссылка, но нужно проверить)

#### 4.5. Добавить роут в App.js
- **Роут:** `<Route path="/artists/:name" element={<ArtistPage />} />`

#### 4.6. Обновить API
- **Файл:** `frontend/src/services/api.js`
- **Добавить:** `getByArtist: (artistName) => api.get(`/albums/artist/${encodeURIComponent(artistName)}`)`

#### 4.7. Создать ArtistPage.css
- Стили для страницы артиста

---

## 🎯 Задача 5: Расширенный поиск

### Backend изменения:

#### 5.1. Расширить GetAlbums
- **Файл:** `backend/controllers/album_controller.go`
- **Уже поддерживает:**
  - ✅ Фильтр по жанру (`genre_id`)
  - ✅ Поиск по названию/артисту (`search`)
  - ✅ Сортировка (`sort_by`, `sort_order`)
  - ⚠️ Нужно добавить сортировку по дате выхода (`release_date`)

#### 5.2. Добавить поле release_date в модель Album (если еще нет)
- Проверить модель `backend/models/album.go`

### Frontend изменения:

#### 5.3. Создать SearchPage.js
- **Файл:** `frontend/src/pages/SearchPage.js`
- **Функционал:**
  - Левая панель с фильтрами:
    - Чекбоксы жанров (загрузить из API)
    - Радио-кнопки сортировки:
      - По дате выхода (новые/старые)
      - По алфавиту (А-Я/Я-А)
      - По рейтингу (высокий/низкий)
      - По дате добавления (новые/старые)
  - Правая часть - результаты поиска (сетка альбомов)
  - Поисковая строка сверху

#### 5.4. Обновить Header.js
- Добавить кнопку "Расширенный поиск" или "Каталог"
- При клике переходить на `/search`

#### 5.5. Обновить HomePage.js
- Текущая страница остается главной
- Можно добавить ссылку на расширенный поиск

#### 5.6. Добавить роут в App.js
- **Роут:** `<Route path="/search" element={<SearchPage />} />`

#### 5.7. Создать SearchPage.css
- Стили для страницы поиска с левой панелью фильтров

#### 5.8. Обновить FilterContext (если нужно)
- Возможно, расширить для поддержки новых фильтров

---

## 🎯 Задача 6: Тестовые данные

### Backend изменения:

#### 6.1. Обновить seedData в database.go
- Добавить больше альбомов для разных артистов (для карточки артиста)
- Добавить больше пользователей с рецензиями (для просмотра профилей)
- Добавить альбомы с разными датами выхода (для сортировки)
- Убедиться, что есть альбомы во всех жанрах (для фильтров)

#### 6.2. Добавить поле release_date в seed
- Если поле есть в модели, заполнить его для всех альбомов

---

## 🎯 Задача 7: Актуализировать PROJECT.md

### Изменения:
- Добавить описание новых страниц (UserProfilePage, ArtistPage, SearchPage)
- Обновить API документацию (новые endpoints)
- Обновить статус проекта
- Добавить скриншоты/описание новых функций

---

## 📝 Порядок выполнения

1. **Задача 3** (профили пользователей) - самая простая, начнем с неё
2. **Задача 4** (карточка артиста) - требует backend изменения
3. **Задача 5** (расширенный поиск) - самая сложная, требует много UI работы
4. **Задача 6** (тестовые данные) - параллельно с остальными
5. **Задача 7** (документация) - в конце

---

## 🔍 Детальный план задачи 3

### Шаг 1: Создать UserProfilePage.js
```javascript
// Структура:
- Получение user по ID из URL
- Отображение аватара, ника, email, даты регистрации
- Список рецензий пользователя
- Без кнопки редактирования
```

### Шаг 2: Обновить ReviewCard.js
```javascript
// Изменения:
- Обернуть username в <Link to={`/users/${review.user.id}`}>
- Обернуть аватар в <Link>
- Добавить hover эффекты
```

### Шаг 3: Добавить роут и стили
- Роут в App.js
- CSS файл для UserProfilePage

---

## 🔍 Детальный план задачи 4

### Шаг 1: Backend - добавить endpoint
```go
// GetAlbumsByArtist в album_controller.go
func (ac *AlbumController) GetAlbumsByArtist(c *gin.Context) {
    artistName := c.Param("name")
    // Декодировать URL encoding
    // Найти все альбомы WHERE artist = ?
    // Preload Genre, Likes
    // Сортировка по release_date или created_at
}
```

### Шаг 2: Frontend - создать ArtistPage.js
```javascript
// Структура:
- Получение artistName из URL
- Загрузка альбомов через API
- Отображение имени артиста
- Сетка альбомов (использовать AlbumCard)
- Статистика
```

### Шаг 3: Сделать имена артистов кликабельными
- Обновить все компоненты, где отображается artist
- Использовать <Link to={`/artists/${encodeURIComponent(artist)}`}>

---

## 🔍 Детальный план задачи 5

### Шаг 1: Проверить/добавить release_date
- Проверить модель Album
- Если нет - добавить миграцию
- Обновить seed данные

### Шаг 2: Backend - расширить сортировку
- Добавить сортировку по release_date в GetAlbums

### Шаг 3: Frontend - создать SearchPage.js
```javascript
// Структура:
<div className="search-page">
  <div className="search-filters-panel">
    {/* Левая панель с фильтрами */}
    <h3>Жанры</h3>
    {/* Чекбоксы жанров */}
    <h3>Сортировка</h3>
    {/* Радио-кнопки */}
  </div>
  <div className="search-results">
    {/* Поисковая строка */}
    {/* Сетка альбомов */}
  </div>
</div>
```

### Шаг 4: Обновить Header
- Добавить ссылку на /search

---

## ✅ Чеклист выполнения

### Задача 3:
- [ ] Создать UserProfilePage.js
- [ ] Создать UserProfilePage.css
- [ ] Обновить ReviewCard.js (кликабельные ники и аватары)
- [ ] Добавить роут в App.js
- [ ] Протестировать переход по никам и аватарам

### Задача 4:
- [ ] Добавить GetAlbumsByArtist в album_controller.go
- [ ] Добавить роут в routes.go
- [ ] Обновить api.js (getByArtist)
- [ ] Создать ArtistPage.js
- [ ] Создать ArtistPage.css
- [ ] Обновить AlbumCard.js (кликабельное имя артиста)
- [ ] Обновить AlbumDetailPage.js
- [ ] Обновить TrackCard.js
- [ ] Добавить роут в App.js
- [ ] Протестировать переход по именам артистов

### Задача 5:
- [ ] Проверить наличие release_date в модели Album
- [ ] Добавить сортировку по release_date в GetAlbums
- [ ] Создать SearchPage.js
- [ ] Создать SearchPage.css
- [ ] Обновить Header.js (кнопка поиска)
- [ ] Добавить роут в App.js
- [ ] Протестировать фильтры и сортировку

### Задача 6:
- [ ] Добавить больше альбомов в seed
- [ ] Добавить больше пользователей
- [ ] Заполнить release_date для альбомов
- [ ] Убедиться в разнообразии жанров

### Задача 7:
- [ ] Обновить PROJECT.md с новыми страницами
- [ ] Обновить API документацию
- [ ] Обновить статус проекта

