# 🚀 Инструкция по установке и запуску

## Требования

- **Go 1.21+** - [Скачать](https://golang.org/dl/)
- **Node.js 16+** - [Скачать](https://nodejs.org/)
- **PostgreSQL 12+** - [Скачать](https://www.postgresql.org/download/)

## Пошаговая установка

### 1. Подготовка базы данных

Убедитесь, что PostgreSQL запущен и доступен.

**Важно:** База данных создается автоматически при первом запуске backend, если её нет. Вам нужно только убедиться, что PostgreSQL запущен и у пользователя есть права на создание БД.

### 2. Настройка Backend

#### 2.1 Перейдите в папку backend

```bash
cd backend
```

#### 2.2 Установите зависимости

```bash
go mod tidy
```

#### 2.3 Создайте файл `.env`

Создайте файл `.env` в папке `backend/`:

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=music_review_db
DB_SSLMODE=disable

# Server Configuration
PORT=8080
GIN_MODE=debug
```

**Важно:** Измените `DB_PASSWORD` на ваш пароль от PostgreSQL, если он отличается.

#### 2.4 Запустите сервер

```bash
go run main.go
```

Backend будет доступен на `http://localhost:8080`

**Примечание:** 
- База данных создается автоматически при первом запуске
- Тестовые данные (пользователи и альбомы) также создаются автоматически
- Если видите ошибку "go.mod file not found", убедитесь, что вы в папке `backend/`

### 3. Настройка Frontend

Откройте **новый терминал** (backend должен продолжать работать):

#### 3.1 Перейдите в папку frontend

```bash
cd frontend
```

#### 3.2 Установите зависимости

```bash
npm install
```

#### 3.3 Создайте файл `.env`

Создайте файл `.env` в папке `frontend/`:

```env
# API Configuration
REACT_APP_API_URL=http://localhost:8080/api

# Webpack Dev Server Configuration (для Windows)
DANGEROUSLY_DISABLE_HOST_CHECK=true
```

#### 3.4 Запустите приложение

```bash
npm start
```

Frontend будет доступен на `http://localhost:3000`

## Тестовые пользователи

После первого запуска автоматически создаются:

- **Администратор:**
  - Email: `admin@example.com`
  - Пароль: `admin123`

- **Тестовый пользователь:**
  - Email: `test@example.com`
  - Пароль: `test123`

## Проверка работы

1. Откройте `http://localhost:3000` в браузере
2. Войдите с тестовыми данными (например, `test@example.com` / `test123`)
3. Проверьте главную страницу с альбомами
4. Откройте альбом и создайте рецензию
5. Войдите как администратор (`admin@example.com` / `admin123`) и проверьте модерацию

## Быстрый запуск (когда всё уже установлено)

Если вы уже настроили проект и просто хотите его запустить:

### Backend
```bash
cd backend
go run main.go
```

### Frontend
```bash
cd frontend
npm start
```

**Примечание:** Убедитесь, что:
- PostgreSQL запущен и доступен
- Файлы `.env` созданы в папках `backend` и `frontend`
- Переменные окружения настроены правильно

## Возможные проблемы

### Backend не запускается

- ✅ Проверьте, что PostgreSQL запущен
- ✅ Проверьте правильность данных в `.env` (особенно `DB_PASSWORD`)
- ✅ Убедитесь, что порт 8080 свободен
- ✅ Проверьте, что вы в папке `backend/` (должен быть файл `go.mod`)

### Frontend не подключается к Backend

- ✅ Проверьте, что Backend запущен на порту 8080
- ✅ Проверьте переменную `REACT_APP_API_URL` в `.env` frontend
- ✅ Проверьте CORS настройки в `backend/main.go`
- ✅ Убедитесь, что оба сервера работают одновременно

### База данных не создается

- ✅ Убедитесь, что у пользователя PostgreSQL есть права на создание БД
- ✅ Проверьте логи в консоли Backend
- ✅ Проверьте подключение к PostgreSQL вручную:
  ```bash
  psql -U postgres -h localhost
  ```

### Ошибки при установке зависимостей

**Go:**
```bash
# Очистите кэш модулей
go clean -modcache
go mod tidy
```

**Node.js:**
```bash
# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json
npm install
```

## Дополнительная информация

- Подробнее о структуре проекта: см. [README.md](../README.md)
- Статус проекта: см. [STATUS.md](../STATUS.md)
- Техническое задание: см. [ТЗ.md](../ТЗ.md)

