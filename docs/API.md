# 🎯 API Endpoints

Полная документация по API endpoints проекта.

## Аутентификация

- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получение информации о текущем пользователе

## Альбомы

- `GET /api/albums` - Получение списка альбомов (с фильтрацией, сортировкой и пагинацией)
- `GET /api/albums/:id` - Получение детальной информации об альбоме (включает жанр, треки, рецензии, лайки)
- `POST /api/albums` - Создание нового альбома (авторизованный пользователь)
- `PUT /api/albums/:id` - Обновление альбома (только админ)
- `DELETE /api/albums/:id` - Удаление альбома (только админ)
- `POST /api/albums/:id/like` - Лайк альбома (авторизованный пользователь)
- `DELETE /api/albums/:id/like` - Убрать лайк с альбома (авторизованный пользователь)

## Рецензии

Рецензии могут быть созданы как для альбомов, так и для треков. При создании рецензии необходимо указать либо `album_id`, либо `track_id` (но не оба одновременно).

- `GET /api/reviews` - Получение списка рецензий (с фильтрацией по `album_id`, `track_id`, `user_id`, `status`)
- `GET /api/reviews/popular` - Получение популярных рецензий (сортировка по количеству лайков)
- `GET /api/reviews/:id` - Получение детальной информации о рецензии
- `POST /api/reviews` - Создание новой рецензии (авторизованный пользователь)
  - Тело запроса: `{ "album_id": uint }` или `{ "track_id": uint }`, оценки, текст (опционально)
- `PUT /api/reviews/:id` - Обновление рецензии (автор или админ)
- `DELETE /api/reviews/:id` - Удаление рецензии (автор или админ)
- `POST /api/reviews/:id/like` - Лайк рецензии (авторизованный пользователь)
- `DELETE /api/reviews/:id/like` - Убрать лайк с рецензии (авторизованный пользователь)
- `POST /api/reviews/:id/approve` - Одобрение рецензии (только админ)
- `POST /api/reviews/:id/reject` - Отклонение рецензии (только админ)

## Жанры

- `GET /api/genres` - Получение списка жанров
- `GET /api/genres/:id` - Получение жанра по ID
- `POST /api/genres` - Создание жанра (только админ)
- `PUT /api/genres/:id` - Обновление жанра (только админ)
- `DELETE /api/genres/:id` - Удаление жанра (только админ)

## Треки

Треки могут иметь несколько жанров (many-to-many связь). При создании/обновлении трека можно указать массив `genre_ids`.

- `GET /api/tracks/popular` - Получение популярных треков за последние 24 часа (сортировка по количеству лайков)
- `GET /api/tracks/:id` - Получение трека по ID (включает альбом, жанры, лайки)
- `GET /api/albums/:id/tracks` - Получение треков альбома (включает жанры и лайки)
- `POST /api/tracks` - Создание трека (только админ)
  - Тело запроса: `{ "album_id": uint, "title": string, "duration": int, "track_number": int, "genre_ids": []uint }`
- `PUT /api/tracks/:id` - Обновление трека (только админ)
  - Тело запроса: можно обновить `title`, `duration`, `track_number`, `genre_ids`
- `DELETE /api/tracks/:id` - Удаление трека (только админ)
- `POST /api/tracks/:id/like` - Лайк трека (авторизованный пользователь)
- `DELETE /api/tracks/:id/like` - Убрать лайк с трека (авторизованный пользователь)

## Поиск

- `GET /api/search?q=query` - Поиск альбомов и треков с автодополнением (возвращает до 5 альбомов и 5 треков)

## Пользователи

- `GET /api/users/:id` - Получение пользователя по ID
- `GET /api/users/:id/reviews` - Получение рецензий пользователя
- `PUT /api/users/:id` - Обновление профиля пользователя
- `DELETE /api/users/:id` - Удаление пользователя

## Авторизация

Для доступа к защищенным эндпоинтам необходимо передавать заголовок `X-User-ID` с ID пользователя. В production рекомендуется использовать JWT токены.

## База URL

Все API endpoints доступны по адресу: `http://localhost:8080/api`

## Работа с изображениями

### Обложки альбомов и треков

Альбомы и треки могут иметь обложки, которые хранятся в поле `cover_image_path`. 

**Формат пути:**
- Путь к изображению указывается относительно корня проекта (например, `/preview/1.jpg`)
- Изображения должны находиться в папке `frontend/public/`
- Фронтенд автоматически обслуживает файлы из папки `public` по корневому пути

**Пример:**
- Путь в базе данных: `/preview/1.jpg`
- Физическое расположение: `frontend/public/preview/1.jpg`
- URL для доступа: `http://localhost:3000/preview/1.jpg`

**Обработка ошибок:**
- Если изображение не найдено или не загружается, отображается placeholder (иконка музыкальной ноты 🎵)
- Все компоненты используют утилиту `getImageUrl()` из `frontend/src/utils/imageUtils.js` для формирования правильных URL

## Формат ответов

API возвращает данные в формате JSON. При ошибках возвращается объект с полем `error`:

```json
{
  "error": "Описание ошибки"
}
```

При успешном выполнении возвращается объект с данными или массив данных.

