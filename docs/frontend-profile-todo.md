# Frontend/profile todo

Priority order for the next diploma-app iterations.

## 1. Profile data model

- Add backend counters for likes given by the user.
- Add backend counter for author likes received by the user.
- Add backend counter for ratings without full reviews.
- Add endpoint for reviews liked by the user, so the `Понравилось` tab is not a placeholder.

## 2. Profile UI

- Keep the RZT-like layout: narrow left column with identity, level and statistics; wide right column with profile content.
- Improve the top showcase with real favorite artists/albums/tracks when more data is available.
- Add editing flow for favorite artists, albums and tracks.
- Polish achievements as calm cards in the app style instead of emoji-heavy labels.

## 3. Leveling

- Base community points on reviews, ratings, received likes, given likes and author likes.
- Keep seasons out of the first version.
- Add clear level thresholds and progress to the next level.
- Replace temporary CSS gems with final level icons if custom assets are prepared.

## 4. Global design

- Continue separating surfaces in the light theme so large blocks do not merge into one beige area.
- Audit desktop pages first, then make tablet/mobile states acceptable.
- Unify icon buttons for likes, edit/delete actions and search/filter controls.
