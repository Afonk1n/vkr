# Диагностика базы данных

## Обзор

Добавлено детальное логирование для диагностики проблем с сидингом базы данных. При запуске сервера автоматически выводится информация о состоянии всех таблиц до и после сидинга.

## Что логируется

### 1. Состояние БД до и после сидинга
- Количество записей в каждой таблице:
  - `users` - пользователи
  - `genres` - жанры
  - `albums` - альбомы
  - `tracks` - треки
  - `track_genres` - связи треков и жанров
  - `reviews` - рецензии
  - `review_likes` - лайки рецензий
  - `track_likes` - лайки треков
  - `album_likes` - лайки альбомов

### 2. Детальное логирование процесса сидинга

#### Жанры (Genres)
- Созданные/существующие жанры с ID
- Общее количество созданных и существующих

#### Пользователи (Users)
- Создание admin и test пользователей с ID
- Создание дополнительных тестовых пользователей
- Статистика по созданным/существующим пользователям

#### Альбомы (Albums)
- Каждый созданный/существующий альбом с ID и GenreID
- Пропущенные альбомы (если GenreID = 0)
- Статистика: создано/существовало/пропущено

#### Треки (Tracks)
- Каждый созданный трек с ID и AlbumID
- Предупреждения о несуществующих альбомах
- Ошибки при создании треков
- Статистика по назначению жанров трекам

#### Рецензии (Reviews)
- Каждая созданная рецензия с ID, UserID, AlbumID/TrackID
- Ошибки при создании
- Статистика по созданным рецензиям и лайкам

#### Лайки (Likes)
- Статистика по созданным/неудачным лайкам для треков и рецензий

## Как использовать

### Автоматическая диагностика
При запуске сервера (`go run backend/main.go`) автоматически выводится:
1. Состояние БД **ДО** сидинга
2. Детальные логи процесса сидинга
3. Состояние БД **ПОСЛЕ** сидинга

### Ручная проверка состояния БД
Можно вызвать функцию диагностики программно:
```go
import "music-review-site/backend/database"

database.LogDatabaseState()
```

## Интерпретация логов

### Успешный сидинг
```
✓ Created genre: Рок (ID: 1)
✓ Created album: Abbey Road by The Beatles (ID: 1, GenreID: 1)
✓ All tables contain data
```

### Проблемы
- `ERROR:` - критическая ошибка, требует внимания
- `WARNING:` - предупреждение, может указывать на проблему
- `Empty tables detected: [albums, tracks]` - пустые таблицы после сидинга

### Типичные проблемы

1. **Пустые таблицы после сидинга**
   - Проверьте логи на `ERROR:` сообщения
   - Убедитесь, что зависимости созданы (например, альбомы требуют жанры)

2. **Ошибки при создании записей**
   - Проверьте ограничения БД (foreign keys, unique constraints)
   - Проверьте валидность данных

3. **Пропущенные записи**
   - Проверьте логи на `WARNING:` о несуществующих зависимостях
   - Убедитесь, что родительские записи созданы

## Пример вывода

```
=== Database state BEFORE seeding ===
📊 Database Statistics:
   Users:       0
   Genres:      0
   Albums:      0
   ...

=== Starting data seeding ===
Seeding initial data...
✓ Created genre: Рок (ID: 1)
✓ Created genre: Поп (ID: 2)
...
✓ Created album: Abbey Road by The Beatles (ID: 1, GenreID: 1)
...

=== Database state AFTER seeding ===
📊 Database Statistics:
   Users:       17
   Genres:      6
   Albums:      5
   Tracks:      60
   ...
✓ All tables contain data
```
