# 🎵 Music Review Site - Сайт рецензирования музыки

Веб-приложение для рецензирования музыкальных альбомов и треков. Пользователи могут просматривать альбомы, оставлять отзывы с детальной системой оценки и взаимодействовать с рецензиями других пользователей.

## 🚀 Технологический стек

### Backend
- **Go (Golang)** - язык программирования
- **Gin** - веб-фреймворк
- **GORM** - ORM для работы с базой данных
- **PostgreSQL** - база данных
- **bcrypt** - хеширование паролей

### Frontend
- **React** - библиотека для создания пользовательских интерфейсов
- **React Router** - маршрутизация
- **Axios** - HTTP клиент
- **CSS** - самописные стили

## 📋 Основные возможности

- ✅ Регистрация и авторизация пользователей
- ✅ Просмотр каталога музыкальных альбомов
- ✅ Фильтрация и сортировка альбомов (по жанру, рейтингу, названию)
- ✅ Детальная система оценки альбомов:
  - Рифмы/Образы (1-10)
  - Структура/Ритмика (1-10)
  - Реализация стиля (1-10)
  - Индивидуальность/Харизма (1-10)
  - Атмосфера/Вайб (множитель 1.0000-1.6072)
- ✅ Создание рецензий с текстом (с модерацией)
- ✅ Автоматический расчет итогового балла
- ✅ Профиль пользователя с личными рецензиями
- ✅ Панель администратора для модерации рецензий
- ✅ Адаптивный дизайн

## 🛠 Быстрый старт

### Требования

- **Go 1.21+** - [Скачать](https://golang.org/dl/)
- **Node.js 16+** - [Скачать](https://nodejs.org/)
- **PostgreSQL 12+** - [Скачать](https://www.postgresql.org/download/)

### Установка и запуск

#### 1. Клонирование и подготовка

```bash
# Перейдите в корневую директорию проекта
cd music-review-site
```

#### 2. Backend

```bash
# Перейдите в папку backend
cd backend

# Установите зависимости Go
go mod tidy

# Создайте файл .env в папке backend и вставьте следующее содержимое:
```

**Содержимое файла `backend/.env`:**
```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=music_review_db
DB_SSLMODE=disable

# Server Configuration
PORT=8080
GIN_MODE=debug
```

**Примечание:** Измените `DB_PASSWORD` на ваш пароль от PostgreSQL, если он отличается.

```bash
# Запустите сервер
go run main.go
```

**Важно:** База данных создается автоматически при первом запуске, если её нет. Тестовые данные (пользователи и альбомы) также создаются автоматически.

Backend будет доступен на `http://localhost:8080`

#### 3. Frontend

Откройте новый терминал:

```bash
# Перейдите в папку frontend
cd frontend

# Установите зависимости
npm install

# Создайте файл .env в папке frontend и вставьте следующее содержимое:
```

**Содержимое файла `frontend/.env`:**
```env
# API Configuration
REACT_APP_API_URL=http://localhost:8080/api

# Webpack Dev Server Configuration
DANGEROUSLY_DISABLE_HOST_CHECK=true
```

```bash
# Запустите приложение
npm start
```

Frontend будет доступен на `http://localhost:3000`

## 🔐 Тестовые пользователи

После первого запуска автоматически создаются тестовые пользователи:

- **Администратор:**
  - Email: `admin@example.com`
  - Пароль: `admin123`

- **Тестовый пользователь:**
  - Email: `test@example.com`
  - Пароль: `test123`

## 📐 Формула расчета оценки

Итоговый балл рассчитывается по формуле:

```
Итоговый балл = (Рифмы + Структура + Реализация + Индивидуальность) × 1.4 × Атмосфера/Вайб
```

Где:
- **Рифмы/Образы** - оценка текста (1-10)
- **Структура/Ритмика** - оценка структуры и ритма (1-10)
- **Реализация стиля** - оценка исполнения и звукорежиссуры (1-10)
- **Индивидуальность/Харизма** - оценка уникальности (1-10)
- **Атмосфера/Вайб** - множитель (1.0000-1.6072)

## 📊 Структура проекта

```
music-review-site/
├── backend/                 # Backend приложение (Go)
│   ├── main.go             # Точка входа
│   ├── .env.example        # Пример переменных окружения
│   ├── routes/             # Маршруты API
│   ├── controllers/        # Контроллеры
│   ├── models/             # Модели данных
│   ├── middleware/         # Middleware
│   ├── database/           # Настройка БД и миграции
│   └── utils/              # Утилиты
├── frontend/               # Frontend приложение (React)
│   ├── .env.example        # Пример переменных окружения
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── pages/          # Страницы
│   │   ├── services/       # API сервисы
│   │   ├── context/        # React Context
│   │   └── utils/          # Утилиты
│   └── public/             # Публичные файлы
└── README.md               # Документация
```

## 🎯 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Альбомы
- `GET /api/albums` - Список альбомов
- `GET /api/albums/:id` - Детали альбома
- `POST /api/albums` - Создание альбома
- `PUT /api/albums/:id` - Обновление альбома
- `DELETE /api/albums/:id` - Удаление альбома

### Рецензии
- `GET /api/reviews` - Список рецензий
- `GET /api/reviews/:id` - Детали рецензии
- `POST /api/reviews` - Создание рецензии
- `PUT /api/reviews/:id` - Обновление рецензии
- `DELETE /api/reviews/:id` - Удаление рецензии
- `POST /api/reviews/:id/approve` - Одобрение рецензии (админ)
- `POST /api/reviews/:id/reject` - Отклонение рецензии (админ)

### Жанры
- `GET /api/genres` - Список жанров
- `GET /api/genres/:id` - Детали жанра
- `POST /api/genres` - Создание жанра (админ)
- `PUT /api/genres/:id` - Обновление жанра (админ)
- `DELETE /api/genres/:id` - Удаление жанра (админ)

## 📝 Особенности

- **Автоматическое создание БД**: База данных создается автоматически при первом запуске
- **Автоматическое заполнение тестовыми данными**: При первом запуске создаются тестовые пользователи и альбомы
- **Модерация рецензий**: Рецензии с текстом отправляются на модерацию и публикуются только после одобрения администратором
- **Автоматический расчет рейтинга**: Средний рейтинг альбома рассчитывается автоматически на основе одобренных рецензий
- **Адаптивный дизайн**: Интерфейс адаптируется под различные размеры экранов
- **Валидация данных**: Полная валидация всех входных данных на backend и frontend

## 🚧 Дальнейшее развитие

- [ ] JWT аутентификация вместо header-based
- [ ] Загрузка обложек альбомов
- [ ] Система лайков для рецензий
- [ ] Комментарии под рецензиями
- [ ] Темная тема
- [ ] Расширенный поиск
- [ ] Рекомендации на основе предпочтений

## ⚡ Быстрый запуск (когда всё уже установлено)

Если вы уже настроили проект и просто хотите его запустить:

### Backend
```bash
cd backend
go run main.go
```

### Frontend
```bash
cd frontend
npm start
```

- **Администратор:**
  - Email: `admin@example.com`
  - Пароль: `admin123`

- **Тестовый пользователь:**
  - Email: `test@example.com`
  - Пароль: `test123`

**Примечание:** Убедитесь, что:
- PostgreSQL запущен и доступен
- Файлы `.env` созданы в папках `backend` и `frontend`
- Переменные окружения настроены правильно

## 📄 Лицензия

Этот проект создан в учебных целях.

## 👤 Автор

Проект создан в рамках выпускной квалификационной работы.
