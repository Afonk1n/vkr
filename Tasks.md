# 📋 Задачи проекта

## ✅ Выполненные задачи

1. ✅ Хочу переработать тёмную тему сайта чтобы она была более современной (сейчас solarized, не стильно), хочу в тёмно серых тонах разных, сам найди подходящие варианты - ВЫПОЛНЕНО

---

## 🎯 Текущие задачи

### 1. Добавить аватарки артистам

**Описание:**
Добавить возможность отображения аватарок (фотографий) для артистов на сайте. Сейчас артисты отображаются только по имени, нужно добавить визуальное представление.

**Текущая ситуация:**
- В модели `Album` есть поле `Artist` (строка с именем)
- Артисты отображаются как текст в карточках альбомов, на страницах альбомов и треков
- Нет связи между артистами и изображениями

**План реализации:**

**Backend:**
1. **Создать модель Artist** (или расширить существующую логику):
   - `ID`, `Name` (уникальное), `AvatarPath`, `Bio`, `CreatedAt`, `UpdatedAt`
   - Связь `Albums []Album` (один артист - много альбомов)
   - Миграция: создать таблицу `artists` или добавить поле `artist_avatar_path` в альбомы

2. **Варианты реализации:**
   - **Вариант А (рекомендуемый):** Создать отдельную таблицу `artists` с полем `avatar_path`
     - Преимущества: нормализация данных, можно добавить биографию артиста, статистику
     - Изменения: добавить `ArtistID` в `Album`, миграция существующих данных
   - **Вариант Б (простой):** Добавить поле `artist_avatar_path` в модель `Album`
     - Преимущества: минимальные изменения, быстро
     - Недостатки: дублирование данных, если у артиста несколько альбомов

3. **API endpoints:**
   - `GET /api/artists` - список артистов
   - `GET /api/artists/:name` - детальная информация об артисте (уже есть `/api/albums/artist/:name`)
   - `PUT /api/artists/:id` - обновление артиста (только админ)

4. **Seed данные:**
   - Добавить аватары для существующих артистов в seed данных
   - Использовать placeholder изображения или реальные обложки

**Frontend:**
1. **Компоненты:**
   - Обновить `AlbumCard.js` - добавить отображение аватара артиста рядом с именем
   - Обновить `ArtistPage.js` - добавить большой аватар артиста в шапке страницы
   - Обновить `AlbumDetailPage.js` - добавить аватар артиста в заголовке альбома

2. **Стили:**
   - Круглые аватары для карточек (40-50px)
   - Большой аватар на странице артиста (150-200px)
   - Placeholder, если аватар отсутствует (иконка или инициалы)

3. **Утилиты:**
   - Использовать существующую `getImageUrl()` для формирования URL аватаров
   - Добавить fallback на placeholder, если изображение не загружается

**Файлы для изменения:**
- `backend/models/artist.go` (новый файл)
- `backend/models/album.go` (добавить связь с Artist)
- `backend/controllers/artist_controller.go` (новый файл)
- `backend/routes/routes.go` (добавить маршруты)
- `backend/database/database.go` (seed данные)
- `frontend/src/components/AlbumCard.js`
- `frontend/src/pages/ArtistPage.js`
- `frontend/src/pages/AlbumDetailPage.js`

---

### 2. Добавить роль для Артистов на сайте и механику лайка на рецензию от артиста

**Описание:**
Создать специальную роль "Артист" для пользователей, которые являются музыкантами. Артисты могут лайкать рецензии на свои альбомы/треки, и такие лайки должны как-то выделяться или отображаться отдельно.

**Текущая ситуация:**
- В модели `User` есть только `IsAdmin` (boolean)
- Система лайков универсальная, нет различия между обычными пользователями и артистами
- Нет связи между пользователем-артистом и артистом из альбомов

**План реализации:**

**Backend:**

1. **Расширить модель User:**
   - Добавить поле `IsArtist bool` (или `Role string` с enum: "user", "admin", "artist")
   - Добавить поле `ArtistName string` (для связи с артистом из альбомов)
   - Связь: один пользователь может быть связан с одним артистом

2. **Создать модель ArtistLike (или расширить ReviewLike):**
   - Вариант А: Добавить поле `IsArtistLike bool` в `ReviewLike`
   - Вариант Б: Создать отдельную таблицу `artist_review_likes` с полями:
     - `UserID`, `ReviewID`, `CreatedAt`
     - Уникальный индекс на `(user_id, review_id)`

3. **Логика проверки:**
   - При лайке рецензии проверять: является ли пользователь артистом
   - Проверять: относится ли рецензия к альбому/треку этого артиста
   - Если да - создавать "лайк артиста" вместо обычного лайка

4. **API endpoints:**
   - `POST /api/reviews/:id/artist-like` - лайк от артиста (или использовать существующий с проверкой)
   - `GET /api/reviews/:id` - добавить в ответ поле `artist_likes_count` и `artist_likes[]`
   - `PUT /api/users/:id` - добавить возможность установки `is_artist` и `artist_name` (только админ или сам пользователь)

5. **Seed данные:**
   - Создать тестового пользователя-артиста (например, "Монеточка")
   - Связать его с артистом из альбомов через `ArtistName`
   - Добавить несколько лайков от артиста на рецензии

**Frontend:**

1. **Визуальное выделение:**
   - **Вариант А:** Отдельная секция "Лайки артистов" в карточке рецензии
   - **Вариант Б:** Специальный бейдж/иконка рядом с лайками (например, ⭐ или 🎵)
   - **Вариант В:** Отдельный счетчик "Артист оценил: X"
   - **Вариант Г:** Выделение лайка артиста другим цветом/иконкой

2. **Компоненты:**
   - Обновить `ReviewCard.js` - добавить отображение лайков артистов
   - Обновить `ReviewCardSmall.js` - компактное отображение
   - Обновить `ProfileEditForm.js` - добавить чекбокс "Я артист" и поле "Имя артиста" (если пользователь сам может стать артистом)

3. **Профиль артиста:**
   - Добавить бейдж "Артист" в профиле
   - Показать статистику: сколько рецензий получил лайки от артиста
   - Список альбомов артиста (если он связан с артистом из БД)

4. **Дополнительный функционал для артистов:**
   - Уведомления о новых рецензиях на их альбомы/треки
   - Возможность отвечать на рецензии (комментарии)
   - Статистика: средний рейтинг альбомов, количество рецензий
   - Возможность загружать аватар артиста (из задачи 1)

**Файлы для изменения:**
- `backend/models/user.go` (добавить `IsArtist`, `ArtistName`)
- `backend/models/review_like.go` (добавить `IsArtistLike` или создать новую модель)
- `backend/controllers/review_controller.go` (логика проверки артиста)
- `backend/controllers/user_controller.go` (обновление роли)
- `backend/database/database.go` (seed данные)
- `frontend/src/components/ReviewCard.js`
- `frontend/src/components/ReviewCardSmall.js`
- `frontend/src/components/ProfileEditForm.js`
- `frontend/src/pages/ProfilePage.js`

---

### 3. Возможность прожимать несколько жанров в каталоге + исправить баг с сортировкой

**Описание:**
Сейчас в каталоге можно выбрать только один жанр. Нужно добавить возможность множественного выбора жанров (AND логика - треки должны принадлежать всем выбранным жанрам, или OR логика - хотя бы одному). Также есть баг: при сортировке всё пропадает и пишет "Ошибка загрузки треков".

**Текущая ситуация:**
- В `SearchPage.js` используется `selectedGenres` (массив), но в API передаётся только первый жанр: `params.genre_id = selectedGenres[0]`
- В `album_controller.go` фильтрация по жанру: `query.Where("genre_id = ?", genreID)` - поддерживает только один жанр
- Треки имеют many-to-many связь с жанрами через `track_genres`, но альбомы имеют только один жанр

**План реализации:**

**Исправление бага с сортировкой:**

1. **Диагностика:**
   - Проверить логику в `SearchPage.js` - возможно, ошибка при изменении `sortBy`
   - Проверить обработку ошибок в `fetchTracks`
   - Проверить, что параметры сортировки корректно передаются в API

2. **Вероятные причины:**
   - Ошибка при парсинге параметров сортировки
   - Проблема с асинхронными запросами (race condition)
   - Ошибка в API при неправильных параметрах

3. **Исправление:**
   - Добавить обработку ошибок с детальными сообщениями
   - Проверить валидацию параметров сортировки
   - Добавить loading state при изменении сортировки

**Множественный выбор жанров:**

**Backend:**

1. **Варианты реализации:**
   - **Вариант А (рекомендуемый):** Фильтрация по трекам с множественными жанрами
     - Изменить логику в `SearchPage.js`: получать треки напрямую через `tracksAPI`
     - Добавить endpoint `GET /api/tracks` с фильтрацией по массиву `genre_ids[]`
     - SQL: `WHERE EXISTS (SELECT 1 FROM track_genres WHERE track_id = tracks.id AND genre_id IN (...))`
   - **Вариант Б:** Фильтрация через альбомы (текущий подход)
     - Проблема: альбом имеет только один жанр, треки могут иметь несколько
     - Решение: получать альбомы по первому жанру, затем фильтровать треки по остальным

2. **API изменения:**
   - `GET /api/tracks` - добавить параметр `genre_ids[]` (массив ID жанров)
   - Логика фильтрации:
     - **AND:** трек должен иметь ВСЕ выбранные жанры
     - **OR:** трек должен иметь ХОТЯ БЫ ОДИН из выбранных жанров
   - По умолчанию использовать AND логику

3. **SQL запрос:**
   ```sql
   -- AND логика (трек должен иметь все жанры)
   SELECT DISTINCT t.* FROM tracks t
   WHERE (
     SELECT COUNT(DISTINCT genre_id) 
     FROM track_genres 
     WHERE track_id = t.id AND genre_id IN (1, 2, 3)
   ) = 3  -- количество выбранных жанров
   ```

**Frontend:**

1. **Компонент Filters:**
   - Изменить `Filters.js` или `FilterModal.js`:
     - Заменить `<select>` на чекбоксы для множественного выбора
     - Отобразить выбранные жанры как теги с возможностью удаления
   - Добавить переключатель AND/OR (опционально)

2. **SearchPage.js:**
   - Изменить логику: использовать `tracksAPI.getAll()` вместо получения через альбомы
   - Передавать массив `genre_ids` в параметрах запроса
   - Обновить состояние `selectedGenres` при изменении фильтров

3. **UI/UX:**
   - Показать количество выбранных жанров
   - Кнопка "Очистить все" для сброса фильтров
   - Визуальное выделение выбранных жанров

**Файлы для изменения:**
- `backend/controllers/track_controller.go` (добавить фильтрацию по genre_ids)
- `frontend/src/pages/SearchPage.js` (исправить баг, изменить логику)
- `frontend/src/components/Filters.js` (множественный выбор)
- `frontend/src/components/FilterModal.js` (множественный выбор)

---

### 4. Добавить отображение обложки в списке треков на странице альбома

**Описание:**
На странице альбома (`AlbumDetailPage.js`) треки отображаются в виде списка без обложек. Нужно добавить миниатюры обложек треков (если они есть) или использовать обложку альбома как fallback.

**Текущая ситуация:**
- В модели `Track` есть поле `CoverImagePath`
- В `AlbumDetailPage.js` треки отображаются в `tracks-list-album` без изображений
- Отображается только номер трека, название, жанры и длительность

**План реализации:**

**Frontend:**

1. **Обновить компонент списка треков:**
   - Добавить `<img>` элемент в `track-item-album`
   - Размер миниатюры: 50-60px (квадрат)
   - Использовать `getImageUrl(track.cover_image_path)` или `getImageUrl(album.cover_image_path)` как fallback

2. **Структура:**
   ```
   <div className="track-item-album">
     <img className="track-item-cover" src={...} />
     <div className="track-item-number">1</div>
     <div className="track-item-info">...</div>
     ...
   </div>
   ```

3. **Стили (AlbumDetailPage.css):**
   - Добавить `.track-item-cover` с фиксированными размерами
   - Скругление углов, object-fit: cover
   - Placeholder, если изображение не загружается

4. **Логика fallback:**
   - Если у трека есть `cover_image_path` - использовать его
   - Если нет - использовать обложку альбома
   - Если и альбома нет - placeholder (🎵 или иконка)

**Backend:**
- Убедиться, что при загрузке треков альбома поле `cover_image_path` включено в ответ
- Проверить seed данные - добавить обложки для некоторых треков (опционально)

**Файлы для изменения:**
- `frontend/src/pages/AlbumDetailPage.js` (добавить изображения)
- `frontend/src/pages/AlbumDetailPage.css` (стили для обложек)

---

### 5. Баг при добавлении телеграма в профиле пользователя выкидывает из аккаунта

**Описание:**
При сохранении профиля с добавлением/изменением поля Telegram пользователь выходит из аккаунта и нужно перезаходить. При этом Telegram добавляется нормально в БД.

**Текущая ситуация:**
- В `ProfilePage.js` при сохранении вызывается `login(user.email, profileData.password || 'dummy')`
- Если пароль не менялся, передаётся 'dummy', что может вызывать ошибку авторизации
- В `AuthContext` может происходить сброс состояния при ошибке

**План реализации:**

**Диагностика:**

1. **Проверить логику в ProfilePage.js:**
   - `handleSaveProfile` вызывает `login()` после обновления
   - Проблема: если пароль не менялся, передаётся 'dummy', что неверно

2. **Проверить AuthContext:**
   - Что происходит при ошибке в `login()`
   - Возможно, при ошибке очищается состояние пользователя

**Исправление:**

1. **Вариант А (рекомендуемый):** Не перелогинивать пользователя
   - После успешного обновления профиля просто обновить `currentUser` в состоянии
   - Обновить `AuthContext` напрямую через метод обновления пользователя
   - Не вызывать `login()` если пароль не менялся

2. **Вариант Б:** Исправить логику перелогина
   - Сохранять текущий пароль перед обновлением
   - Использовать сохранённый пароль для перелогина
   - Или: если пароль не менялся, не вызывать `login()`, а просто обновить контекст

3. **Изменения в ProfilePage.js:**
   ```javascript
   const handleSaveProfile = async (profileData) => {
     try {
       const response = await usersAPI.update(user.id, profileData);
       setCurrentUser(response.data);
       setIsEditing(false);
       
       // Обновить AuthContext без перелогина
       if (updateUser) {
         updateUser(response.data);
       }
       
       alert('Профиль успешно обновлен');
     } catch (err) {
       // обработка ошибок
     }
   };
   ```

4. **Добавить метод в AuthContext:**
   - `updateUser(userData)` - обновить данные пользователя в контексте без перелогина

**Файлы для изменения:**
- `frontend/src/pages/ProfilePage.js` (исправить handleSaveProfile)
- `frontend/src/context/AuthContext.js` (добавить updateUser метод)

---

### 6. Заменить инстаграмм на MAX

**Описание:**
В профиле пользователя есть поле для Instagram, нужно заменить его на MAX (новый музыкальный сервис).

**Текущая ситуация:**
- В модели `User` поле `SocialLinks` хранит JSON: `{"vk": "", "telegram": "", "instagram": ""}`
- В `ProfileEditForm.js` есть поле для Instagram
- Отображается в `ProfilePage.js` и `UserProfilePage.js`

**План реализации:**

**Изменения:**

1. **Backend (опционально):**
   - Можно оставить структуру JSON как есть, просто изменить ключ
   - Или добавить миграцию для переименования ключа в существующих данных

2. **Frontend:**
   - `ProfileEditForm.js`: заменить поле "Instagram" на "MAX"
   - `ProfilePage.js`: заменить отображение Instagram на MAX
   - `UserProfilePage.js`: заменить отображение Instagram на MAX
   - Обновить иконки/логотипы (если используются)

3. **Валидация:**
   - Обновить регулярное выражение для валидации URL MAX (если нужно)
   - Формат URL: `https://max.ru/...` или `@username`

**Файлы для изменения:**
- `frontend/src/components/ProfileEditForm.js`
- `frontend/src/pages/ProfilePage.js`
- `frontend/src/pages/UserProfilePage.js`

---

### 7. Реализовать возможность загружать аватарки

**Описание:**
Добавить функционал загрузки аватарок для пользователей. Сейчас можно только указать путь к аватару вручную через `avatar_path`.

**Текущая ситуация:**
- В модели `User` есть поле `AvatarPath`
- В `ProfileEditForm.js` есть текстовое поле для `avatar_path`
- Нет функционала загрузки файлов

**План реализации:**

**Backend:**

1. **Настройка хранения файлов:**
   - Создать папку `backend/uploads/avatars/` для хранения аватаров
   - Или использовать `frontend/public/avatars/` для прямого доступа
   - Рекомендуется: `frontend/public/avatars/` (проще для доступа из браузера)

2. **API endpoint:**
   - `POST /api/users/:id/avatar` - загрузка аватара
   - Принимает multipart/form-data с полем `avatar`
   - Валидация: размер файла (макс 5MB), формат (jpg, png, webp)
   - Генерация уникального имени файла: `user_{id}_{timestamp}.{ext}`
   - Сохранение файла и обновление `avatar_path` в БД
   - Удаление старого аватара при загрузке нового

3. **Обработка файлов:**
   - Использовать библиотеку для работы с изображениями (опционально)
   - Ресайз изображения до разумного размера (например, 400x400px)
   - Сохранение в разных размерах (thumbnail, medium, large) - опционально

4. **Безопасность:**
   - Проверка MIME типа файла
   - Ограничение размера файла
   - Санитизация имени файла

**Frontend:**

1. **Компонент загрузки:**
   - Заменить текстовое поле на `<input type="file">`
   - Превью загруженного изображения перед сохранением
   - Кнопка "Загрузить" или автоматическая загрузка при выборе файла
   - Индикатор загрузки

2. **ProfileEditForm.js:**
   - Добавить состояние для файла и preview
   - Обработчик `onChange` для input file
   - Функция `handleAvatarUpload` для отправки на сервер
   - Отображение текущего аватара и preview нового

3. **Стили:**
   - Круглый preview аватара
   - Стилизованная кнопка выбора файла
   - Индикатор загрузки

4. **Обработка ошибок:**
   - Валидация на клиенте (размер, формат)
   - Отображение ошибок от сервера

**Файлы для изменения:**
- `backend/controllers/user_controller.go` (добавить UploadAvatar метод)
- `backend/routes/routes.go` (добавить маршрут)
- `frontend/src/components/ProfileEditForm.js` (загрузка файлов)
- `frontend/src/components/ProfileEditForm.css` (стили)

**Зависимости:**
- Возможно, понадобится библиотека для работы с изображениями в Go (например, `github.com/disintegration/imaging`)

---

### 8. Продумать систему званий пользователей в зависимости от жанров рецензий

**Описание:**
Создать систему, которая будет присваивать пользователям "звания" или "бейджи" в зависимости от того, треки каких жанров они рецензируют. Например: "Джазовый критик", "Поп-эксперт", "Рок-ценитель" и т.д.

**Текущая ситуация:**
- В модели `Review` есть связь с `Album` и `Track`
- У `Track` есть many-to-many связь с `Genre`
- У `Album` есть связь с одним `Genre`
- Нет системы званий/бейджей

**План реализации:**

**Концепция:**

1. **Типы званий:**
   - **По жанру:** "Джазовый критик", "Поп-эксперт", "Рок-ценитель", "Электронный знаток"
   - **По количеству:** "Начинающий критик" (1-5 рецензий), "Опытный критик" (6-20), "Мастер рецензий" (21+)
   - **По разнообразию:** "Универсал" (рецензии на 5+ разных жанров)
   - **Комбинированные:** "Джазовый мастер" (10+ рецензий на джаз)

2. **Правила присвоения:**
   - Вычислять звания динамически при запросе профиля
   - Или хранить в БД и обновлять при создании рецензии
   - Приоритет: более специфичные звания важнее общих

**Backend:**

1. **Модель (опционально):**
   - Создать таблицу `user_badges` для хранения званий пользователей
   - Или вычислять динамически (рекомендуется для начала)

2. **Логика вычисления:**
   - Функция `CalculateUserBadges(userID)`:
     - Получить все рецензии пользователя
     - Для каждой рецензии определить жанр (через Album.Genre или Track.Genres)
     - Подсчитать количество рецензий по каждому жанру
     - Применить правила присвоения званий
     - Вернуть массив званий с приоритетами

3. **API:**
   - `GET /api/users/:id/badges` - получить звания пользователя
   - Или включить в `GET /api/users/:id` поле `badges[]`

4. **Структура звания:**
   ```json
   {
     "name": "Джазовый критик",
     "description": "10+ рецензий на джазовые треки",
     "icon": "🎷",
     "priority": 1
   }
   ```

**Frontend:**

1. **Отображение в профиле:**
   - Бейджи под аватаром пользователя
   - Иконка + название звания
   - При наведении - описание

2. **Компоненты:**
   - `Badge.js` - компонент одного бейджа
   - `BadgeList.js` - список бейджей
   - Обновить `ProfilePage.js` и `UserProfilePage.js`

3. **Стили:**
   - Красивые бейджи с градиентами
   - Разные цвета для разных типов званий
   - Анимация при наведении

4. **Дополнительно:**
   - Прогресс-бар до следующего звания
   - История получения званий
   - Редкие/особые звания (например, "Первопроходец" - первая рецензия на новый альбом)

**Примеры званий:**

- **По жанрам (5+ рецензий):**
  - 🎷 Джазовый критик
  - 🎤 Поп-эксперт
  - 🎸 Рок-ценитель
  - 🎹 Электронный знаток
  - 🎺 Блюзовый знаток
  - 🥁 Хип-хоп критик

- **По количеству:**
  - 🌱 Начинающий критик (1-5)
  - 📝 Опытный критик (6-20)
  - ⭐ Мастер рецензий (21-50)
  - 👑 Легенда критики (51+)

- **По разнообразию:**
  - 🌈 Универсал (5+ разных жанров)
  - 🎯 Специалист (80%+ рецензий в одном жанре)

**Файлы для изменения:**
- `backend/models/user.go` (добавить метод CalculateBadges или отдельная модель)
- `backend/controllers/user_controller.go` (логика вычисления)
- `frontend/src/components/Badge.js` (новый компонент)
- `frontend/src/components/BadgeList.js` (новый компонент)
- `frontend/src/pages/ProfilePage.js`
- `frontend/src/pages/UserProfilePage.js`

---

## 📝 Примечания

- Задачи расположены в порядке приоритета (по мнению автора)
- Некоторые задачи можно выполнять параллельно
- Рекомендуется начать с задач 3 (баг) и 5 (баг), так как они критичны для пользовательского опыта
- Задачи 1, 4, 6, 7 - относительно простые, можно выполнять в любом порядке
- Задачи 2 и 8 - более сложные, требуют продуманной архитектуры


### 9. Улучшить визуал формы оценки - выделить группировку оценок и множитель

**Описание:**
Улучшить визуальное оформление формы оценки (`ReviewForm.js`), чтобы было понятно, что 4 базовые оценки (Рифмы, Структура, Реализация, Индивидуальность) формируют одну группу, которая затем умножается на множитель "Атмосфера/Вайб". Добавить визуальные блоки, фон или обводку для лучшего понимания структуры формулы.

**Текущая ситуация:**
- Все 5 оценок отображаются в одном списке без визуального разделения
- Формула: `(Рифмы + Структура + Реализация + Индивидуальность) × 1.4 × Атмосфера`
- Нет визуального указания на то, что первые 4 оценки - это одна группа
- Нет визуального указания на то, что Атмосфера - это множитель

**План реализации:**

**Концепция визуализации:**

1. **Вариант А (рекомендуемый):** Визуальная группировка с блоками
   - Обернуть 4 базовые оценки в блок с фоном/обводкой
   - Показать заголовок "Базовые критерии (суммируются)"
   - Атмосферу вынести в отдельный блок с заголовком "Множитель"
   - Показать формулу визуально: `(Σ базовых) × 1.4 × Атмосфера = Итоговый балл`

2. **Вариант Б:** Тонкая обводка и разделитель
   - Добавить тонкую обводку вокруг 4 базовых оценок
   - Добавить визуальный разделитель (линия) между базовыми и множителем
   - Использовать разные оттенки фона для групп

3. **Вариант В:** Карточки/плитки
   - Базовые оценки в одной карточке (grid 2x2)
   - Атмосфера в отдельной карточке
   - Итоговый балл в отдельной карточке с формулой

**Frontend изменения:**

1. **Структура компонента:**
   ```jsx
   <div className="rating-section">
     {/* Блок базовых оценок */}
     <div className="base-ratings-group">
       <div className="group-header">
         <h4>Базовые критерии</h4>
         <span className="group-summary">
           Сумма: {ratingRhymes + ratingStructure + ratingImplementation + ratingIndividuality}
         </span>
       </div>
       <div className="base-ratings-grid">
         {/* 4 оценки в grid */}
       </div>
     </div>
     
     {/* Разделитель */}
     <div className="rating-divider">
       <span>× 1.4 ×</span>
     </div>
     
     {/* Блок множителя */}
     <div className="multiplier-group">
       <div className="group-header">
         <h4>Атмосфера / Вайб (множитель)</h4>
       </div>
       {/* Атмосфера */}
     </div>
     
     {/* Итоговый балл с формулой */}
     <div className="final-score-block">
       <div className="formula">
         ({ratingRhymes} + {ratingStructure} + {ratingImplementation} + {ratingIndividuality}) 
         × 1.4 × {atmosphereRating} = {finalScore}
       </div>
       <div className="final-score-value">
         Итоговый балл: {formatScore(finalScore)}
       </div>
     </div>
   </div>
   ```

2. **Стили (ReviewForm.css):**

   **Блок базовых оценок:**
   ```css
   .base-ratings-group {
     background: var(--card-background);
     border: 2px solid var(--border-color);
     border-radius: var(--radius-lg);
     padding: 1.5rem;
     position: relative;
   }
   
   .base-ratings-group::before {
     content: '';
     position: absolute;
     top: 0;
     left: 0;
     right: 0;
     height: 3px;
     background: linear-gradient(90deg, 
       var(--primary-color), 
       var(--secondary-color)
     );
     border-radius: var(--radius-lg) var(--radius-lg) 0 0;
   }
   
   .group-header {
     display: flex;
     justify-content: space-between;
     align-items: center;
     margin-bottom: 1rem;
     padding-bottom: 0.75rem;
     border-bottom: 1px solid var(--border-color);
   }
   
   .group-summary {
     font-size: 0.875rem;
     color: var(--text-secondary);
     font-weight: 600;
   }
   
   .base-ratings-grid {
     display: grid;
     grid-template-columns: repeat(2, 1fr);
     gap: 1rem;
   }
   
   @media (max-width: 768px) {
     .base-ratings-grid {
       grid-template-columns: 1fr;
     }
   }
   ```

   **Разделитель:**
   ```css
   .rating-divider {
     display: flex;
     align-items: center;
     justify-content: center;
     padding: 1rem;
     position: relative;
   }
   
   .rating-divider::before,
   .rating-divider::after {
     content: '';
     flex: 1;
     height: 1px;
     background: var(--border-color);
   }
   
   .rating-divider span {
     padding: 0 1rem;
     color: var(--text-secondary);
     font-weight: 600;
     font-size: 1.125rem;
   }
   ```

   **Блок множителя:**
   ```css
   .multiplier-group {
     background: var(--card-background);
     border: 2px solid var(--primary-color);
     border-radius: var(--radius-lg);
     padding: 1.5rem;
     position: relative;
   }
   
   .multiplier-group::before {
     content: '×';
     position: absolute;
     top: -12px;
     left: 1.5rem;
     background: var(--card-background);
     padding: 0 0.5rem;
     color: var(--primary-color);
     font-size: 1.5rem;
     font-weight: bold;
   }
   ```

   **Итоговый балл:**
   ```css
   .final-score-block {
     background: linear-gradient(135deg, 
       var(--primary-color), 
       var(--secondary-color)
     );
     border-radius: var(--radius-lg);
     padding: 1.5rem;
     text-align: center;
     color: white;
   }
   
   .formula {
     font-size: 0.875rem;
     opacity: 0.9;
     margin-bottom: 0.5rem;
     font-family: 'Courier New', monospace;
   }
   
   .final-score-value {
     font-size: 1.5rem;
     font-weight: 700;
   }
   ```

3. **Альтернативный вариант (более тонкий):**
   - Использовать только обводку без фона
   - Более тонкие границы (1px вместо 2px)
   - Более мягкие цвета
   - Минималистичный подход

4. **Анимация (опционально):**
   - Плавное появление блоков
   - Подсветка при изменении значений
   - Анимация итогового балла при изменении

**UX улучшения:**

1. **Интерактивность:**
   - При изменении базовых оценок - подсветка суммы
   - При изменении атмосферы - подсветка множителя
   - Показывать изменение итогового балла в реальном времени

2. **Подсказки:**
   - Добавить иконку "?" рядом с формулой с объяснением
   - Показывать диапазон возможных значений
   - Визуализация веса каждого критерия

3. **Адаптивность:**
   - На мобильных: вертикальная компоновка
   - Grid базовых оценок: 2x2 на десктопе, 1x4 на мобильном
   - Упрощённая формула на маленьких экранах

**Файлы для изменения:**
- `frontend/src/components/ReviewForm.js` (изменить структуру)
- `frontend/src/components/ReviewForm.css` (добавить стили для группировки)

**Дополнительные идеи:**

1. **Визуализация формулы:**
   - Показывать промежуточные вычисления
   - Анимация при изменении значений
   - График распределения оценок

2. **Цветовое кодирование:**
   - Разные цвета для разных групп критериев
   - Градиент для итогового балла в зависимости от значения

3. **Прогресс-бары:**
   - Визуальные индикаторы для каждой оценки
   - Общий прогресс-бар для суммы базовых оценок