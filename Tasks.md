# 📋 Задачи проекта

## 🔧 Актуальные задачи

### Критичные баги (приоритет 1)

#### 1. ✅ Исправить генерацию лайков - одинаковые значения вместо вариативности - **ВЫПОЛНЕНО**
**Проблема:** 
- В рецензиях везде по 16 лайков кроме последней (там 5)
- В треках везде по 17 лайков
- В "Актуальное" все релизы без лайков
- Нужны разные цифры в диапазоне 5-30 для каждого элемента

**Причина:** Формула `i * 13 % 26` и `trackIndex * 7 % 26` дают повторяющиеся значения из-за простого модуля.

**План исправления:**
- Использовать более сложную формулу с комбинацией индекса и ID элемента
- Для треков: `5 + ((trackIndex * 7 + track.ID) % 26)`
- Для рецензий: `5 + ((i * 13 + review.ID) % 26)`
- Убедиться, что лайки на альбомы тоже генерируются (сейчас только для первых 3-4 альбомов)
- Проверить, что лайки на альбомы учитываются в блоке "Актуальное" (релизы)

**Файлы для изменения:**
- `backend/database/database.go` (функции `seedTrackLikes`, `seedReviewLikes`, добавить `seedAlbumLikes`)

---

#### 2. ✅ Исправить сортировку в каталоге - работает только по алфавиту - **ВЫПОЛНЕНО**
**Проблема:**
- Сортировка работает только по алфавиту (title_asc/desc)
- Остальные сортировки (по дате, рейтингу, лайкам) не работают
- Дублирование опции "По рейтингу (низкий)" в списке

**План исправления:**
- Проверить backend контроллер `track_controller.go` - поддерживает ли он все типы сортировки
- Проверить параметры запроса в `SearchPage.js`
- Убрать дублирование опции сортировки
- Протестировать все варианты сортировки

**Файлы для изменения:**
- `frontend/src/pages/SearchPage.js` (убрать дубликат, проверить параметры)
- `backend/controllers/track_controller.go` (проверить логику сортировки)

---

### Улучшения тестовых данных (приоритет 2)

#### 3. ✅ Оптимизировать список жанров - оставить 15 самых популярных в РФ - **ВЫПОЛНЕНО**
**Задача:** 
- Оставить только самые популярные жанры в РФ за последние годы (штук 15)
- Треки, которые уже используют другие жанры, не трогать (оставить как есть)
- В будущем можно будет добавить треки к новым жанрам

**План реализации:**
- Проанализировать текущие жанры в БД
- Определить 15 самых популярных жанров в РФ (Поп, Рэп, Хип-хоп, Рок, Электронная, Инди-поп, Альтернативный рок и т.д.)
- Удалить неиспользуемые жанры (если они не используются в треках)
- Обновить список жанров в seed данных

**Файлы для изменения:**
- `backend/database/database.go` (функция `seedGenres`)

**Примечание:** Нужно быть осторожным - не удалять жанры, которые используются в существующих треках.

---

### Тестирование (приоритет 3)

#### 4. ✅ Проверить реализацию лайков на альбомы - **ВЫПОЛНЕНО**
**Результат проверки:**
- ✅ Модель `AlbumLike` существует в backend
- ✅ Endpoints для лайков на альбомы работают (`POST /api/albums/:id/like`, `DELETE /api/albums/:id/like`)
- ✅ Компонент `LikeButton` поддерживает альбомы
- ✅ Страница альбома использует `LikeButton` для лайков
- ⚠️ Блок "Релизы" на главной странице показывает последние альбомы, а не популярные по лайкам

**Примечание:** Лайки на альбомы работают, но в блоке "Актуальное" (Релизы) используются последние альбомы по дате создания, а не популярные по лайкам. Для исправления нужно либо создать endpoint `/api/albums/popular`, либо изменить логику получения релизов.

---

#### 5. ⏸️ Тестирование на мобильных устройствах - **ОТЛОЖЕНО**
**Задача:** 
- Протестировать адаптивность интерфейса на мобильных устройствах
- Проверить все основные страницы и компоненты
- Исправить найденные проблемы с отображением

**Статус:** Отложено на потом

---

## 📝 Комментарии и примечания

### О лайках
- **Лайки на альбомы:** Сейчас создаются только для первых 3-4 альбомов (admin и testUser). Нужно добавить генерацию лайков для всех альбомов с вариативностью 5-30.
- **Лайки на треки:** Формула даёт повторяющиеся значения. Нужно использовать комбинацию индекса и ID трека.
- **Лайки на рецензии:** Та же проблема - формула даёт одинаковые значения. Нужно улучшить формулу.

### О сортировке
- Backend должен поддерживать сортировку по `created_at`, `release_date`, `title`, `average_rating`, `likes_count`
- Frontend отправляет правильные параметры, но возможно backend их не обрабатывает корректно
- Нужно проверить SQL запросы в `track_controller.go`

### О жанрах
- Текущие жанры в тестовых данных: нужно проанализировать, какие используются
- Не удалять жанры, которые уже используются в треках
- Оставить только популярные в РФ жанры (15 штук)

### О тестировании
- Лайки на альбомы могут быть не реализованы в интерфейсе, хотя модель есть
- Мобильная версия требует проверки на реальных устройствах

---

## 🎯 План действий (приоритет выполнения)

1. **СРОЧНО:** Исправить генерацию лайков (задача #1) - влияет на тестирование "Актуальное"
2. **СРОЧНО:** Исправить сортировку в каталоге (задача #2) - критичный баг функциональности
3. **ВАЖНО:** Проверить лайки на альбомы (задача #4) - нужно понять, что не работает
4. **СРЕДНЕ:** Оптимизировать жанры (задача #3) - улучшение данных
5. ⏸️ **ОТЛОЖЕНО:** Мобильное тестирование (задача #5) - проверка адаптивности

---

## 💡 Идеи на будущее

- Поиск пользователей и добавление в друзья
- Система комментариев под рецензиями
- Улучшение системы рекомендаций
- Расширенная аналитика для пользователей


